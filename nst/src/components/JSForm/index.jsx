import React from "react";
import { getEvent } from "../../event/Event";

export default class JSForm extends React.Component{
    render(){
        return(
            <div>
                form
            </div>
        )
    }
}